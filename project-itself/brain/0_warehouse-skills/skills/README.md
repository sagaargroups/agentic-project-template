# 🎯 Individual Skills

Single-purpose AI agent skills. Each skill has its own folder with a `SKILL.md`.

```
skills/
└── my-skill/
    └── SKILL.md    # YAML frontmatter (name, description) + instructions
```

**Standard:** Follow the SKILL.md format defined in `.agents/skills/global-context-blueprint/`.
