# 🔧 Individual Tools

Single-purpose helper utilities. Each tool has its own folder with a `README.md` and source files.

```
tools/
└── my-tool/
    ├── README.md         # What it does, how to run it
    ├── [source files]    # The actual tool
    └── .env.example      # Required env vars (if any)
```
